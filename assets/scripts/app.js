const defaultValue = 0
let currenResult = defaultValue
currenResult = (currenResult + 10) * 3/2 -1
let calculationDescription = (`(${defaultValue} + 10) * 3/2 -1`)
outputResult(currenResult,calculationDescription)